# Database (PostgreSQL + PostGIS)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/taxip_db

# Redis
REDIS_URL=redis://localhost:6379/0

# Security (JWT + bcrypt)
SECRET_KEY=your-super-secret-key-change-in-production-minimum-32-characters
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Email (SMTP para recuperación de contraseña)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu_email@gmail.com
SMTP_PASSWORD=tu_contraseña
SMTP_FROM=no-reply@taxip.com

# Google Maps API (geocoding + distance matrix)
GOOGLE_MAPS_API_KEY=tu_api_key_aqui

# MercadoPago (stub inicial)
MERCADOPAGO_ACCESS_TOKEN=tu_token_test

# Frontend URL (CORS)
FRONTEND_URL=http://localhost:3000

# Environment
ENVIRONMENT=development
DEBUG=true